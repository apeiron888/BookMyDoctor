version: '3.8'

services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - hams-network

  redis:
    image: redis:7
    networks:
      - hams-network

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - MONGODB_URI=mongodb+srv://alleoo2004_db_user:KTrgMijalzmZWtc4@bookmydoctor.4jlrkyu.mongodb.net/?appName=BookMyDoctor
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=bookmydoctor_jwt_secret_2026
      - JWT_SECRET_KEY=bookmydoctor_jwt_secret_2026
      - JWT_EXPIRES=7d
      - COOKIE_EXPIRE=7
      - ADMIN_EMAIL=admin@bookmydoctor.com
      - ADMIN_PASSWORD=admin123456
      - CLOUDINARY_CLOUD_NAME=dh1zghoh2
      - CLOUDINARY_SECRET_KEY=A4wnNBuogpvybJ-fOqWSEFLYtnA
      - CLOUDINARY_API_KEY=632251871385414
      - FRONTEND_URL=http://localhost:5173
      - DASHBOARD_URL=http://localhost:5174
    depends_on:
      - mongodb
      - redis
    networks:
      - hams-network

  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_BACKEND_URL=http://localhost:4000
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - hams-network

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
      args:
        - VITE_BACKEND_URL=http://localhost:4000
    ports:
      - "5174:5174"
    depends_on:
      - backend
    networks:
      - hams-network

volumes:
  mongo-data:

networks:
  hams-network:
    driver: bridge
